*agents.nvim*                     Provider-neutral coding agents in Neovim

==============================================================================
INTRODUCTION                                                     *agents-intro*

agents.nvim runs a coding-agent CLI in a Neovim terminal while leaving session
persistence with the native provider. The initial provider is Claude Code.

==============================================================================
SETUP                                                               *agents-setup*

>lua
  require("agents").setup({
    default_provider = "claude",
    cwd = nil,
    window = { position = "right", size = 0.4 },
    mappings = {
      toggle = "<C-f><C-f>",
      escape = "<C-f>f",
    },
    providers = {
      claude = { command = "claude", args = {} },
    },
  })
<

The toggle mapping shows or hides the agent window without stopping its job. It
works from Normal mode and the agent terminal. The escape mapping is local to
the agent terminal and enters Terminal-Normal mode; press |i| to return to
Terminal mode. Set a mapping to false to disable it.

==============================================================================
COMMANDS                                                         *agents-commands*

                                                   *:Agents-open*
:Agents open [provider]
    Start a new session with the provider.

                                                   *:Agents-resume*
:Agents resume [session-id]
    Resume a session. With Claude, omitting the ID opens its native picker.

                                                   *:Agents-continue*
:Agents continue
    Continue the most recent conversation for the current directory.

                                                   *:Agents-toggle*
:Agents toggle
    Show or hide the active agent terminal.

                                                   *:Agents-stop*
:Agents stop
    Stop the active job and remove its terminal buffer.

                                                   *:Agents-health*
:Agents health
    Report whether configured providers are executable.

==============================================================================
PROVIDERS                                                       *agents-providers*

A provider implements is_available(config) and
build_command(action, options, config). build_command returns a list whose first
element is the executable and remaining elements are its arguments.

vim:tw=78:ts=8:noet:ft=help:norl:
